
?pdhpi=observe for verdicts
?pdhpi=envelope for wrapper state

---

### 8. `index.php`

Your **full PDHPI v2** script — the large PHP file you already have.  
Save it as `index.php` in the project root.

---

### After all files exist

On GitHub when creating the repo:

- **README?** → No  
- **.gitignore?** → No  
- **License?** → No  

Then on your computer:

```bash
cd pdhpi
git init
git add .
git commit -m "Initial release: PDHPI v2"
git branch -M main
git remote add origin https://github.com/Ping192sbs/pdhpi.git
git push -u origin main

